# Scripts

Project-level utility scripts for Posh Bubbles.

## Usage

```bash
# Run a script from the project root
node scripts/<script-name>.mjs
```

## Available Scripts

| Script | Description |
|---|---|
| _(none yet)_ | Add scripts here as the project grows |

Suggested scripts to add:
- `generate-sitemap.mjs` — build a sitemap from product/collection routes
- `import-catalogue.mjs` — seed catalogue data from a CSV or CMS export
- `optimize-images.mjs` — batch-compress assets in `public/images/`
